services:
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:3032
    command: npm run dev -- --hostname 0.0.0.0 -p 3000

  backend:
    build: ./backend
    ports:
      - "3032:3032"
    volumes:
      - ./backend:/app
    command: uvicorn server:app --host 0.0.0.0 --port 3032 --reload
